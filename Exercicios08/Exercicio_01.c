#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[]){
    printf("Char: %d\nInt: %d\nFloat: %d\nDouble: %d", sizeof(char), sizeof(int), sizeof(float), sizeof(double));
    return 0;
}